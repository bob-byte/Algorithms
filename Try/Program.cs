using System;
using Algorithms;
using AlgorithmsSort;

namespace Try
{
    class Program : AlgorithmsBase<Int32>
    {
        static void Main(string[] args)
        {
            //AlgorithmsBase<Int32> list = new AlgorithmsBase<int>();
            //Console.Write("Input amount of elements: ");
            //Int32 length = Convert.ToInt32(Console.ReadLine());
            //for (int i = 0; i < length; i++)
            //{
            //    Console.Write($"list[{i + 1}] = ");
            //    Int32 item = Convert.ToInt32(Console.ReadLine());
            //    list.Items.Add(item);
            //}
            //BubbleSort<Int32> bubble = new BubbleSort<int>();
            //bubble.Sort(list.Items);
            //for (int i = 0; i < length; i++)
            //{
            //    if(i != length - 1)
            //    {
            //        Console.Write($"{list.Items[i]}, ");
            //    }
            //    else
            //    {
            //        Console.Write($"{list.Items[i]}");
            //    }
            //}
            //Console.ReadKey();
        }
    }
}